// SSR or CSR?

// Act as a charities card list view

const Explore = () => {
    // import user profile to determine if an admin (changes buttons)
    // charities state
    // isLoading state?
    // Loading message state

    // useEffect to get charities & set them to variable. Also include a .catch to setLoadingMessage

    // title banner: "Browse and Support Your Favorite Cause"
    // filter button (to be imported from components): Filter by Cause
    // admin only button: "Add Organization"
    // map through charity cards (import card component)
    

}


export default Explore